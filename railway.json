{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput --settings=expensetracker.settings_production"
    },
    "deploy": {
        "startCommand": "gunicorn expensetracker.wsgi:application --settings=expensetracker.settings_production",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}